?pickup.distance < 15
    equipL star

?foe.distance > 18
    equipR ouroboro
:
    loadout 3 

?item.GetCooldown("mask") <= 0 & foe.count > 0
    equipR mask
    ?item.CanActivate("mask")
        activate R

